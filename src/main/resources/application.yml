server:
  port: ${PORT:8080}

spring:
  application:
    name: fossil-finder

fossil:
  data:
    directory: classpath:data/

openweather:
  api:
    key: ${OPENWEATHER_API_KEY}
    base-url: https://api.openweathermap.org/data/2.5

logging:
  level:
    com.fossilfinder: INFO
